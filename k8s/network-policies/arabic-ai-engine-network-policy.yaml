apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: arabic-ai-engine-network-policy
  namespace: rasid
  labels:
    rasid.io/module: "arabic-ai-engine"
    rasid.io/version: "6.4"
spec:
  podSelector:
    matchLabels:
      app: arabic-ai-engine
  policyTypes: [Ingress, Egress]
  ingress:
    - from:
        - podSelector:
            matchLabels:
              app: m30-gateway
        - podSelector:
            matchLabels:
              rasid.io/tier: kernel
      ports:
        - port: 3000
          protocol: TCP
  egress:
    - to:
        - podSelector:
            matchLabels:
              app: arabic-ai-engine-db
      ports:
        - port: 5432
          protocol: TCP
    - to:
        - podSelector:
            matchLabels:
              rasid.io/tier: kernel
      ports:
        - port: 3000
          protocol: TCP
    - to: []
      ports:
        - port: 53
          protocol: UDP
