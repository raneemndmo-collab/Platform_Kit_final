apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: bi-cognitive-network-policy
  namespace: rasid
  labels:
    rasid.io/module: "bi-cognitive"
    rasid.io/version: "6.4"
spec:
  podSelector:
    matchLabels:
      app: bi-cognitive
  policyTypes: [Ingress, Egress]
  ingress:
    - from:
        - podSelector:
            matchLabels:
              app: m30-gateway
        - podSelector:
            matchLabels:
              rasid.io/tier: kernel
      ports:
        - port: 3000
          protocol: TCP
  egress:
    - to:
        - podSelector:
            matchLabels:
              app: bi-cognitive-db
      ports:
        - port: 5432
          protocol: TCP
    - to:
        - podSelector:
            matchLabels:
              rasid.io/tier: kernel
      ports:
        - port: 3000
          protocol: TCP
    - to: []
      ports:
        - port: 53
          protocol: UDP
